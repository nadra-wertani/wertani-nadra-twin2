import { FirstlettreupperPipe } from './firstlettreupper.pipe';

describe('FirstlettreupperPipe', () => {
  it('create an instance', () => {
    const pipe = new FirstlettreupperPipe();
    expect(pipe).toBeTruthy();
  });
});
